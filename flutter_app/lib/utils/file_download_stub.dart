void descargarArchivo({
  required String nombre,
  required List<int> bytes,
  required String mimeType,
}) {
  throw UnsupportedError('La descarga de archivos no está disponible en esta plataforma.');
}
